Active guardrails every skill reads at its own Pre-flight step.

Populated by `meta-synthesis`, which proposes new guardrails here after
detecting a pattern that's repeated across 2 or more sessions in
`/context/skill-sessions.md`, gated on explicit user approval. See the
`meta-synthesis` skill for the full process.

Empty until the first guardrail is approved.
